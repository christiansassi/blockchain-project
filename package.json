{
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.22.9"
    },

    "scripts": {
        "test": "node utils/clear_screen.js && npx hardhat test ./test/tests.js",
        "test:build": "node utils/clear_screen.js && npx hardhat clean && npx hardhat compile --force && npx hardhat test ./test/tests.js"
    }
}
