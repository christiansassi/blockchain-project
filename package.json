{
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.22.9"
    },
    "scripts": {
        "compile": "npx hardhat clean && npx hardhat compile --force",
        "tests": "node utils/modules/utils.js && node ./tests/run-all.js",
        "tests:compile": "node utils/modules/utils.js && npx hardhat clean && npx hardhat compile --force && node ./tests/run-all.js",
        "demo": "node utils/modules/utils.js && npx hardhat clean && node utils/launcher.js"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.3.0",
        "@walletconnect/sign-client": "^2.10.4-184f2234",
        "log-update": "^2.0.0",
        "ora": "^6.0.1",
        "web3": "^1.8.1"
    }
}
